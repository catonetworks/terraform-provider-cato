package provider

// Shared PolicyRulePositionEnum string values used by bulk and TLS inspection movers.
const (
	ifwRulePositionFirstInSection = "FIRST_IN_SECTION"
	ifwRulePositionAfterRule      = "AFTER_RULE"
)
